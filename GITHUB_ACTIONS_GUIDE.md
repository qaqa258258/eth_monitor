# GitHub Actions 配置指南

## 🚀 使用GitHub Actions自动运行监控

### 优势
- ✅ 24/7自动运行，无需本地电脑
- ✅ GitHub服务器在国外，无需代理
- ✅ 免费（每月2000分钟）
- ✅ 可定时触发或手动触发
- ✅ 自动保存信号历史

### 劣势
- ⚠️ 运行频率受限（建议15分钟以上）
- ⚠️ 不是实时监控，会有延迟
- ⚠️ 长时间运行可能超出免费额度

---

## 📋 配置步骤

### 1. 准备仓库

确保这些文件已存在：
- `.github/workflows/monitor.yml` - Actions工作流
- `run_once.py` - 单次运行脚本
- 其他所有项目文件

### 2. 配置GitHub Secrets

在GitHub仓库设置中添加以下Secrets：

**路径**: `Settings` → `Secrets and variables` → `Actions` → `New repository secret`

#### 必需的Secrets：

| Secret名称 | 值 | 说明 |
|-----------|-----|------|
| `TELEGRAM_BOT_TOKEN` | `8530556154:AAFr5Dhy1iXabnskMcx6H2nlqaWpOBYWbTQ` | Telegram Bot Token |
| `TELEGRAM_CHAT_ID` | `5785662116` | Telegram Chat ID |

#### 可选的Secrets（使用默认值可不设置）：

| Secret名称 | 默认值 | 说明 |
|-----------|--------|------|
| `TRADING_SYMBOL` | `ETH/USDT` | 交易对 |
| `TIMEFRAME` | `15m` | 时间周期 |
| `BOLL_PERIOD` | `20` | BOLL周期 |
| `BOLL_STD_DEV` | `2.0` | BOLL标准差 |
| `RSI_PERIOD` | `14` | RSI周期 |
| `RSI_OVERBOUGHT` | `70` | RSI超买线 |
| `RSI_OVERSOLD` | `30` | RSI超卖线 |

### 3. 修改工作流文件（可选）

如果要使用默认值，修改 `.github/workflows/monitor.yml`：

```yaml
- name: 创建配置文件
  run: |
    cat > config.json << EOF
    {
      "proxy": "",
      "symbol": "ETH/USDT",
      "timeframe": "15m",
      "check_interval": 60,
      "boll": {
        "period": 20,
        "std_dev": 2.0
      },
      "rsi": {
        "period": 14,
        "overbought": 70,
        "oversold": 30
      },
      "telegram": {
        "bot_token": "${{ secrets.TELEGRAM_BOT_TOKEN }}",
        "chat_id": "${{ secrets.TELEGRAM_CHAT_ID }}"
      }
    }
    EOF
```

### 4. 调整运行频率

修改 `.github/workflows/monitor.yml` 中的 cron表达式：

```yaml
on:
  schedule:
    - cron: '*/15 * * * *'  # 每15分钟运行
```

**常用频率：**
- `*/15 * * * *` - 每15分钟
- `*/30 * * * *` - 每30分钟
- `0 * * * *` - 每小时
- `0 */4 * * *` - 每4小时
- `0 0 * * *` - 每天午夜

⚠️ **注意**: 不建议设置太频繁（如每分钟），容易超出GitHub Actions限额

---

## 🔧 使用方法

### 自动运行
提交代码后，GitHub Actions会按照设置的cron表达式自动运行

### 手动触发
1. 进入仓库的 `Actions` 标签页
2. 选择 `ETH交易信号监控` 工作流
3. 点击 `Run workflow` 按钮
4. 点击绿色的 `Run workflow` 确认

### 查看运行结果
1. 进入 `Actions` 标签页
2. 点击最近的运行记录
3. 查看日志输出
4. 下载信号历史（Artifacts）

---

## 🔐 安全注意事项

### ✅ 安全做法
- ✅ 使用GitHub Secrets存储敏感信息
- ✅ 不要在代码中硬编码API密钥
- ✅ `.gitignore` 已配置，本地 `config.json` 不会上传
- ✅ 工作流文件使用 `${{ secrets.XXX }}` 引用密钥

### ⚠️ 注意事项
- Secrets在日志中会被自动隐藏（显示为 `***`）
- 确保仓库是Private，或者不要输出Secrets
- 定期检查GitHub Actions的运行日志

---

## 📊 监控运行状态

### 查看日志
```
Actions → 选择运行记录 → 点击 monitor → 查看步骤日志
```

### 查看信号历史
```
Actions → 选择运行记录 → Artifacts → 下载 signals-history
```

### 设置通知
GitHub会在Actions失败时发邮件通知你

---

## 💰 GitHub Actions 免费额度

**免费账户:**
- 每月 2000 分钟
- 并发任务: 20个

**计算示例:**
- 每次运行约1分钟
- 每15分钟运行一次 = 每小时4次 = 每天96次 = 每月约2880次
- 每月消耗约 2880 分钟 ⚠️ 会超额！

**建议:**
- 设置为每30分钟或每小时运行
- 或者在特定时间段运行（如交易时间）

### 限制运行时间段（可选）

只在UTC 0-8点（北京时间8-16点）运行：
```yaml
on:
  schedule:
    - cron: '*/30 0-8 * * *'  # 每30分钟，仅在UTC 0-8点
```

---

## 🐛 故障排查

### Actions运行失败
1. 检查Secrets是否正确设置
2. 查看运行日志中的错误信息
3. 手动触发测试

### 没收到Telegram消息
1. 检查 `TELEGRAM_BOT_TOKEN` 和 `TELEGRAM_CHAT_ID`
2. 确认信号检测逻辑是否触发
3. 查看Actions日志中的推送状态

### 超出免费额度
1. 降低运行频率
2. 使用GitHub Pro（每月3000分钟）
3. 使用其他CI/CD服务

---

## 📝 完整设置检查清单

- [ ] 创建 `.github/workflows/monitor.yml`
- [ ] 创建 `run_once.py`
- [ ] 上传代码到GitHub
- [ ] 设置 `TELEGRAM_BOT_TOKEN` Secret
- [ ] 设置 `TELEGRAM_CHAT_ID` Secret
- [ ] （可选）设置其他参数Secrets
- [ ] 调整cron表达式
- [ ] 手动触发测试
- [ ] 检查Telegram是否收到消息
- [ ] 查看Actions日志确认正常

---

## 🎯 推荐配置

**对于初学者:**
```yaml
# 每小时运行一次
schedule:
  - cron: '0 * * * *'
```

**对于长期使用:**
```yaml
# 每30分钟运行一次（不超免费额度）
schedule:
  - cron: '*/30 * * * *'
```

**对于高频交易:**
```yaml
# 每15分钟运行一次（可能超额度）
schedule:
  - cron: '*/15 * * * *'
```

✅ 配置完成后，你的监控系统将自动运行并推送信号到Telegram！
